def moveZeroes(self, nums: List[int]) -> None:
        i=j=0
        while i < len(nums):
            i += 1
            if nums[j] == 0:
                nums.append(nums[j])
                del(nums[j])
                continue
            j += 1
Time Complexity is O(n)
Space Complexity is O(1)
